# Environment variable NAMES only - never commit values.
# The probe reads the key from the named variable at run time and logs only the name.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
FIREWORKS_API_KEY=
# ... plus one per provider you want to measure
